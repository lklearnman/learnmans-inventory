# 矿珍库交接 · 2026-05-19 · 本位币双轨改造

## TL;DR

PR [#6](https://github.com/lklearnman/learnmans-inventory/pull/6) 已 merge,merge commit `046c33f`,Vercel `v=20260519` 上线。Git tag `v1.0-baseccy` 标记此版本为"初版"。

## 本次做了什么

### 数据库(Supabase `learnmanLight`)
- `logs` 表 schema 变更:
  - `price` → `original_price`(重命名,text)
  - `currency` → `original_currency`(重命名,text)
  - 新增 `base_price`(numeric,本位单价,冻结)
  - 新增 `base_currency`(text default 'JPY')
  - 新增 `fx_rate`(numeric,当时汇率,冻结)
- `products.currency` 全部 backfill 为 'JPY'(字段保留)
- 27 条历史 logs 全部 backfill(汇率:CNY=23.28 / USD=158.55 / EUR=184.36,frankfurter 2026-05-15 收盘)
- 备份表 `logs_backup_20260519` / `products_backup_20260519` 留底(确认稳定后可手动 DROP)

### 代码(`v1.0-baseccy`)
- `inventory-core.js`:加 `BASE_CURRENCY='JPY'` 常量;`dbToLog` 双向暴露原始/本位字段 + 兼容老 `price`/`currency` 别名;`insertLog` 写入 6 字段;新增 `getFxRate(from,to)` helper
- `inventory.html`:顶部 stats 栏新增全局币种 select;删除库存 toolbar / 建品 modal 的币种 select;标签 modal `<option selected>` 去除
- `inventory-ui.js`:`openAddModal`/`openEditModal`/`saveProduct` 去 f-currency;`doStockIn`/`doOut` 录入时算 `base_price = original_price * fx_rate` 并冻结
- `inventory-ai.js`:`openStockOutModal` 默认单价 = `p.price`
- `inventory-labels.js`:`openLabelModal` 打开时默认币种 = `inventoryCurrency`
- `inventory-logs.js`:`renderSummary`/`renderLogsTable`/`printLogs` 合计改用 `basePrice` 累加 + 换算;`exportLogCSV` 表头 11 列(原始单价/原始币种/汇率/本位单价/小计本位);`renderStats` 各处改用 `originalPrice||price`
- 缓存版本号:`v=20260519`

## 已知未修问题(用户 2026-05-19 22:00 反馈,本次 session 上限不做)

### 🔴 必须修(下次 session 优先)

**P1. 手机版顶部全局币种 select 看不到**
- 用户截图显示:nav-tabs 旁有空白区域,header-stats 整个隐藏
- 原因:[inventory.css:156](inventory.css:156) 有 `@media .header-stats{display:none;}`(估计是 480px 以下隐藏)— 改造前手机就不显示 header-stats,现在加进去的全局币种 select 也跟着被隐藏
- 修复方向:把全局币种 select 移出 `.header-stats`,改放到 `.nav-tabs` 末尾(或 nav-tabs 后面单独一行右对齐)。注意 .nav-tabs 是 `overflow-x:auto`,select 需要 `position:sticky` 或单独容器
- 涉及文件:[inventory.html:25-29](inventory.html:25)(把 select 移走)+ [inventory.css:155-160](inventory.css:155)(media query)

**P2. 标签打印 PDF 商品名乱码**
- 用户截图:商品名变成 `„Ý"»jDi„eË•IW W` 之类,价格 `¥13,968 JP` 正常
- 原因:jsPDF 默认 latin 字体不支持中文。[inventory-labels.js:4](inventory-labels.js:4) 注释已提到此问题,只对币种符号做了 ASCII fallback,**商品名没处理**
- 修复方向:三选一
  1. 给 jsPDF 加中文字体(添加 `noto-sans-sc.ttf`,~2MB,影响首次加载)
  2. 改用 HTML2Canvas + jsPDF.addImage 把 HTML 渲染成图片放进 PDF(图片化,不挑字体但放大失真)
  3. 改用浏览器 `window.print()` 路径(交给用户系统的打印对话框,系统字体管中文)
- ⚠️ 这是改造**之前**就有的老问题,本次改造没引入,但用户现在才反馈
- 涉及文件:[js/inventory-labels.js](js/inventory-labels.js) 的 PDF 生成部分

**P3. 入/出库 CSV 和打印里"项目好多没有"**
- 用户截图:出库打印 8 列(日期/商品名/SKU/类别/数量/单价/小计/备注)
- CSV 已经 11 列(原始单价/原始币种/汇率/本位单价/小计本位 + 原 8 列减去单价小计)
- 不一致:打印的列没跟 CSV 同步扩展
- 修复方向:`printLogs` 表格表头改为和 CSV 一样的 11 列(或留 8 列但加币种后缀让单价更清晰)— 看用户具体期望
- 涉及文件:[js/inventory-logs.js:279](js/inventory-logs.js:279)(printLogs 表头 + 数据列)

**P4. 打印前应有"价格确认"环节**
- 用户:打印标签前希望确认商品当前价格
- 修复方向:标签打印 modal 内已显示「将打印的商品」列表,可在每行加上当前价格 + 一个"修改"按钮(可选)。或更轻量:列表项后追加 `<span>${fmtPriceRaw(p.price,'JPY')}</span>`
- 涉及文件:[js/inventory-labels.js](js/inventory-labels.js) 标签 modal 列表渲染

### 🟡 待办(优先级中)

- **统计页币种说明文字过时**:[inventory.html:225](inventory.html:225) "货币跟随库存页" 文字依然合适
- **CLAUDE.md 多币种章节过时**:描述"每条 logs 一个 currency"已不符,改造后是"双轨"。建议下次 session 一并更新 CLAUDE.md(顺手)
- **删 2 条测试 logs**(用户在 Supabase 自己跑):
  ```sql
  DELETE FROM logs WHERE counterparty IN ('测试供应商A', '测试客户B');
  ```

### 💤 长期(CLAUDE.md 紧急/重要,均未做)

- PWA(主屏图标 + 离线)
- 用户登录(Google + 角色权限)
- 精臣打印机标签
- Excel 通用导入
- AI 识别速度排查
- 照片改存 Google Drive
- Mac Mini 备份 Supabase

## 关键文件对照

| 文件 | 本次改动 | 行号锚点 |
|---|---|---|
| [inventory.html](inventory.html) | 全局币种 select + 删旧 select | 25-29 加 / 60-63 删 / 260-267 改 |
| [inventory.css](inventory.css) | 未改;但 @media 隐藏 header-stats 是 P1 根因 | 156 |
| [js/inventory-core.js](js/inventory-core.js) | BASE_CURRENCY / dbToLog / insertLog / getFxRate | 119-177 / 217-223 |
| [js/inventory-ui.js](js/inventory-ui.js) | modal 去 f-currency / doStockIn/doOut 算 base | 167-205 / 303-342 |
| [js/inventory-ai.js](js/inventory-ai.js) | openStockOutModal 默认单价 | 337-357 |
| [js/inventory-labels.js](js/inventory-labels.js) | openLabelModal 默认币种 | 20-32 |
| [js/inventory-logs.js](js/inventory-logs.js) | renderSummary / printLogs / CSV / stats 改 basePrice | 41 / 124 / 189 / 223 / 394+ |

## 验证清单(P1-P4 修完后跑)

桌面 + 手机各点一遍:
1. ✅ 顶部 stats 栏切币种(P1 修完后手机也看得到),库存/详情/流水金额跟随
2. ✅ 库存页 toolbar 旧币种 select 已消失(已验证)
3. ✅ 建品 modal 价格下方「币种: JPY」label,无 select
4. ⚠️ 入库选 CNY 输 100 → Supabase logs 末条 `base_price ≈ 2328`(未实机验证,下次自己开 supabase 看)
5. ✅ 出库 modal 默认单价 = 商品价格(已实机:用户截图显示"¥200 円")
6. ✅ 流水合计数字(已实机:出库合计 ¥200 円按 JPY 显示)
7. ⚠️ CSV 11 列(未实机)
8. ❌ 打印 8 列(应同步扩展,见 P3)
9. ⚠️ 标签打印默认币种(modal 已显示 JPY,但 P2 字体问题盖过)

## Git 状态

- main 最新:`046c33f`(PR #6 merge)
- tag:`v1.0-baseccy` 指向 `046c33f`
- worktree 分支:`claude/vigorous-margulis-f56892`(已删除或保留)
- 备份表:Supabase `logs_backup_20260519` / `products_backup_20260519`

## 用户偏好回顾(自动 memory 已记)

- 中文简体回复
- 手机+浏览器操作为主,F12 可指导
- Claude 可自动 push / PR / merge(这台机器 git 操作已全部静默授权)
- 死板地"等授权"会浪费用户时间
- 多币种改造时:UI 上多个币种 select 让人困惑,务必收口
- 出错信息要直接显示在 UI 里,不要让用户翻 Console
